@{
  Layout = "_Layout";
}

@model BestRestaurants.Models.Restaurant

<div class="well">
  <h2>Are you sure you want to delete this?</h2>

  <p class="delete">@Html.DisplayNameFor(model => model.Name): @Html.DisplayFor(model => model.Name)</p>
  @using (Html.BeginForm())
  {
    <input type="submit" id="delete" class="btn btn-block" value="Delete" />
  }
  <div class="fake-button well">
    <center><p  class="link">@Html.ActionLink("Back to List", "Index")</p></center>
  </div>
</div>